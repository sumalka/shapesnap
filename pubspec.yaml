name: shapesnap
description: "Offline-first mobile app for body shape classification and style guidance for women."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.8.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.9
  
  # UI & State Management
  provider: ^6.1.5+1
  
  # Camera & Image Selection
  camera: ^0.12.0+1
  image_picker: ^1.2.2
  
  # Local Database
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.5
  shared_preferences: ^2.5.5
  path: ^1.9.1
  image: ^4.9.1

  # Firebase Services
  firebase_core: ^4.9.0
  firebase_auth: ^6.5.1
  firebase_analytics: ^12.4.1
  firebase_database: ^12.4.1
  cloud_firestore: ^6.7.1

  # Navigation
  go_router: ^17.2.3

  # Utilities
  intl: ^0.20.2
  permission_handler: ^12.0.2

  # Audio & Animations
  audioplayers: ^6.7.0
  lottie: ^3.3.3
  just_audio: ^0.10.5

  # Premium Fonts
  google_fonts: ^8.1.0

  # High-Resolution Assets (Splash Screen & App Icons)
  flutter_native_splash: ^2.4.8
  flutter_launcher_icons: ^0.14.4
  rive: ^0.14.7
  tflite_flutter: ^0.12.1
  http: ^1.6.0
  connectivity_plus: ^7.3.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Code generation for Hive
  build_runner: ^2.15.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - assets/logo.png
    - assets/logo1.png
    - assets/model/shapesnap_model.tflite
    - assets/model/labels.txt

    # DO and DON'T recommendations
    - assets/recommendations/do/apple/
    - assets/recommendations/do/hourglass/
    - assets/recommendations/do/inverted_triangle/
    - assets/recommendations/do/pear/
    - assets/recommendations/do/rectangle/

    - assets/recommendations/dont/apple/
    - assets/recommendations/dont/hourglass/
    - assets/recommendations/dont/inverted_triangle/
    - assets/recommendations/dont/pear/
    - assets/recommendations/dont/rectangle/

    # CASUAL wear - all body shapes
    - assets/recommendations/casual/apple/
    - assets/recommendations/casual/hourglass/
    - assets/recommendations/casual/pear/
    - assets/recommendations/casual/rectangle/
    - assets/recommendations/casual/inverted_triangle/

    # PARTY wear
    - assets/recommendations/party/apple/
    - assets/recommendations/party/hourglass/
    - assets/recommendations/party/pear/
    - assets/recommendations/party/rectangle/
    - assets/recommendations/party/inverted_triangle/

    # OFFICE wear
    - assets/recommendations/office/apple/
    - assets/recommendations/office/hourglass/
    - assets/recommendations/office/pear/
    - assets/recommendations/office/rectangle/
    - assets/recommendations/office/inverted_triangle/

    # WEDDING wear
    - assets/recommendations/wedding/apple/
    - assets/recommendations/wedding/hourglass/
    - assets/recommendations/wedding/pear/
    - assets/recommendations/wedding/rectangle/
    - assets/recommendations/wedding/inverted_triangle/

    # GYM wear
    - assets/recommendations/gym/apple/
    - assets/recommendations/gym/hourglass/
    - assets/recommendations/gym/pear/
    - assets/recommendations/gym/rectangle/
    - assets/recommendations/gym/inverted_triangle/

    # Audio files
    - assets/sounds/sparkle.mp3

    # Animation files
    - assets/animations/body_scan.json
    - assets/animations/fashion.json
    - assets/animations/privacy.json
    - assets/animations/arrow.json
    - assets/animations/girl_animation.json
    - assets/animations/loading.json
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

#  NATIVE SPLASH SCREEN CONFIGURATION
flutter_native_splash:
  color: "#E91E63"
  image: assets/logo.png
  android: true
  ios: true
  android_gravity: center
  ios_content_mode: center
  fullscreen: true

# APP ICON CONFIGURATION
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/logo1.png"
  adaptive_icon_background: "#E91E63"
  adaptive_icon_foreground: "assets/logo1.png"